@model CoreShop.Domain.Entities.Manufacturer;

@using (Html.BeginForm("Edit","Manufacturer",Model,FormMethod.Post,true,new {@class = "form justify-content-center"}))
{
    
    @Html.AntiForgeryToken();

    <div class="form-group row"  >
        @Html.LabelFor(Model=>Model.ManufacturerId, new {@class = "control-label col-md-4"})
        <div>
            @Html.DisplayFor(Model=>Model.ManufacturerId, new {@class ="form-control col"})
        </div>
    </div>

    <div class="form-group row" >
        @Html.LabelFor(Model=>Model.ManufacturerName, new {@class = "control-label col-md-4"}) 
        <div>
            @Html.EditorFor(Model=>Model.ManufacturerName, new {@class ="form-control col"})
            @Html.ValidationMessageFor(Model=>Model.ManufacturerName,"", new {@class = "text-danger"})
        </div>
    </div>

    <div class="form-actions">
        <input type="submit" value="Save" class="btn btn-large btn-primary">
        @Html.ActionLink("Cancel","Index","Manufacturer",new{}, new{@class="btn btn-large btn-secondary"})
    </div>
}
